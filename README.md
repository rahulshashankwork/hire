# Resume Formatting Application

A lightweight resume formatting application with a **single predefined template**.

## Features
- Fixed resume layout (users only provide content)
- Live update of formatted resume
- Print / Save as PDF support using browser print dialog

## Run locally
```bash
python3 -m http.server 8000
```
Then open `http://localhost:8000`.
